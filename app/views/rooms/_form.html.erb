<%= form_for(@message) do |f| %>
    <%= f.text_area :message, placeholder: "Please enter your message..." , size: 70, class:"form-text-field" %>
    <%= f.hidden_field :room_id, value: @room.id %>
    <%= f.submit "送信", class: 'form-submit' 'btn btn-success' %>
<% end %>