<% provide(:title, "Edit dog") %>
<% provide(:url, dog_path) %>
<% provide(:btn_text, "編集") %>

<h1> <%=@dog.name%> のプロフィール編集</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= render 'dogs/form' %>
    </div>
</div>