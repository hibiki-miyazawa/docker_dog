<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4 jumbotron">
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <h3>いいね一覧</h3>
    <% if @microposts %>
        <ol class="microposts scroll height-fixed">
            <%= render @microposts %>
        </ol>
    <% end %>
  </div>
</div>